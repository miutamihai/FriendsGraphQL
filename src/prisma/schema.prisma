datasource db {
  provider = "sqlite"
  url="file:./friends"
}

generator client {
  provider = "prisma-client-js"
}

model Friend {
  id Int @id @default(autoincrement())
  firstName String
  lastName String
}
